# GENERATED by codegen/generate.py — DO NOT EDIT.
# generated by datamodel-codegen:

from __future__ import annotations

from enum import Enum, StrEnum
from typing import Any

from pydantic import AwareDatetime, BaseModel, Field, constr


class InfoItem(BaseModel):
    name: str
    value: str


class InitiatedBy(BaseModel):
    automated: bool | None = Field(
        default=None,
        description='Automated is set to true if operation was initiated automatically by the application controller.',
    )
    username: str | None = Field(
        default=None,
        description='Username contains the name of a user who started operation',
    )


class Backoff(BaseModel):
    duration: str | None = Field(
        default=None,
        description='Duration is the amount to back off. Default unit is seconds, but could also be a duration (e.g. "2m", "1h")',
    )
    factor: int | None = Field(
        default=None,
        description='Factor is a factor to multiply the base duration after each failed retry',
    )
    maxDuration: str | None = Field(
        default=None,
        description='MaxDuration is the maximum amount of time allowed for the backoff strategy',
    )


class Retry(BaseModel):
    backoff: Backoff | None = Field(
        default=None,
        description='Backoff controls how to backoff on subsequent retries of failed syncs',
    )
    limit: int | None = Field(
        default=None,
        description='Limit is the maximum number of attempts for retrying a failed sync. If set to 0, no retries will be performed.',
    )
    refresh: bool | None = Field(
        default=None,
        description='Refresh indicates if the latest revision should be used on retry instead of the initial one (default: false)',
    )


class Resource(BaseModel):
    group: str | None = None
    kind: str
    name: str
    namespace: str | None = None


class ExtVar(BaseModel):
    code: bool | None = None
    name: str
    value: str


class Tla(BaseModel):
    code: bool | None = None
    name: str
    value: str


class Jsonnet(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class FileParameter(BaseModel):
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    path: str | None = Field(
        default=None,
        description='Path is the path to the file containing the values for the Helm parameter',
    )


class Parameter(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Target(BaseModel):
    annotationSelector: str | None = None
    group: str | None = None
    kind: str | None = None
    labelSelector: str | None = None
    name: str | None = None
    namespace: str | None = None
    version: str | None = None


class Patch(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Replica(BaseModel):
    count: int | str = Field(..., description='Number of replicas')
    name: str = Field(..., description='Name of Deployment or StatefulSet')


class Kustomize(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class EnvItem(BaseModel):
    name: str = Field(
        ...,
        description='Name is the name of the variable, usually expressed in uppercase',
    )
    value: str = Field(..., description='Value is the value of the variable')


class Parameter1(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter1] | None = None


class Source(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class Jsonnet1(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory1(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet1 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter2(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm1(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter2] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch1(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize1(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch1] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter3(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin1(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter3] | None = None


class Source1(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory1 | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm1 | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize1 | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin1 | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class Apply(BaseModel):
    force: bool | None = Field(
        default=None,
        description='Force indicates whether or not to supply the --force flag to `kubectl apply`.\nThe --force flag deletes and re-create the resource, when PATCH encounters conflict and has\nretried for 5 times.',
    )


class Hook(BaseModel):
    force: bool | None = Field(
        default=None,
        description='Force indicates whether or not to supply the --force flag to `kubectl apply`.\nThe --force flag deletes and re-create the resource, when PATCH encounters conflict and has\nretried for 5 times.',
    )


class SyncStrategy(BaseModel):
    apply: Apply | None = Field(
        default=None,
        description='Apply will perform a `kubectl apply` to perform the sync.',
    )
    hook: Hook | None = Field(
        default=None,
        description='Hook will submit any referenced resources to perform the sync. This is the default strategy',
    )


class Sync(BaseModel):
    autoHealAttemptsCount: int | None = Field(
        default=None,
        description='SelfHealAttemptsCount contains the number of auto-heal attempts',
    )
    dryRun: bool | None = Field(
        default=None,
        description='DryRun specifies to perform a `kubectl apply --dry-run` without actually performing the sync',
    )
    manifests: list[str] | None = Field(
        default=None,
        description='Manifests is an optional field that overrides sync source with a local directory for development',
    )
    prune: bool | None = Field(
        default=None,
        description='Prune specifies to delete resources from the cluster that are no longer tracked in git',
    )
    resources: list[Resource] | None = Field(
        default=None,
        description='Resources describes which resources shall be part of the sync',
    )
    revision: str | None = Field(
        default=None,
        description='Revision is the revision (Git) or chart version (Helm) which to sync the application to\nIf omitted, will use the revision specified in app spec.',
    )
    revisions: list[str] | None = Field(
        default=None,
        description='Revisions is the list of revision (Git) or chart version (Helm) which to sync each source in sources field for the application to\nIf omitted, will use the revision specified in app spec.',
    )
    source: Source | None = Field(
        default=None,
        description='Source overrides the source definition set in the application.\nThis is typically set in a Rollback operation and is nil during a Sync operation',
    )
    sources: list[Source1] | None = Field(
        default=None,
        description='Sources overrides the source definition set in the application.\nThis is typically set in a Rollback operation and is nil during a Sync operation',
    )
    syncOptions: list[str] | None = Field(
        default=None,
        description='SyncOptions provide per-sync sync-options, e.g. Validate=false',
    )
    syncStrategy: SyncStrategy | None = Field(
        default=None, description='SyncStrategy describes how to perform the sync'
    )


class Operation(BaseModel):
    info: list[InfoItem] | None = Field(
        default=None,
        description='Info is a list of informational items for this operation',
    )
    initiatedBy: InitiatedBy | None = Field(
        default=None,
        description='InitiatedBy contains information about who initiated the operations',
    )
    retry: Retry | None = Field(
        default=None, description='Retry controls the strategy to apply if a sync fails'
    )
    sync: Sync | None = Field(
        default=None, description='Sync contains parameters for the operation'
    )


class Destination(BaseModel):
    name: str | None = Field(
        default=None,
        description='Name is an alternate way of specifying the target cluster by its symbolic name. This must be set if Server is not set.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace specifies the target namespace for the application's resources.\nThe namespace will only be set for namespace-scoped resources that have not set a value for .metadata.namespace",
    )
    server: str | None = Field(
        default=None,
        description="Server specifies the URL of the target cluster's Kubernetes control plane API. This must be set if Name is not set.",
    )


class IgnoreDifference(BaseModel):
    group: str | None = None
    jqPathExpressions: list[str] | None = None
    jsonPointers: list[str] | None = None
    kind: str
    managedFieldsManagers: list[str] | None = Field(
        default=None,
        description="ManagedFieldsManagers is a list of trusted managers. Fields mutated by those managers will take precedence over the\ndesired state defined in the SCM and won't be displayed in diffs",
    )
    name: str | None = None
    namespace: str | None = None


class Jsonnet2(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory2(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet2 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter4(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm2(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter4] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch2(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize2(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch2] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter5(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin2(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter5] | None = None


class Source2(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory2 | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm2 | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize2 | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin2 | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class Jsonnet3(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory3(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet3 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter6(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm3(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter6] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch3(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize3(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch3] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter7(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin3(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter7] | None = None


class DrySource(BaseModel):
    directory: Directory3 | None = Field(
        default=None, description='Directory specifies path/directory specific options'
    )
    helm: Helm3 | None = Field(
        default=None, description='Helm specifies helm specific options'
    )
    kustomize: Kustomize3 | None = Field(
        default=None, description='Kustomize specifies kustomize specific options'
    )
    path: str = Field(
        ...,
        description='Path is a directory path within the Git repository where the manifests are located',
    )
    plugin: Plugin3 | None = Field(
        default=None,
        description='Plugin specifies config management plugin specific options',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the git repository that contains the application manifests',
    )
    targetRevision: str = Field(
        ..., description='TargetRevision defines the revision of the source to hydrate'
    )


class HydrateTo(BaseModel):
    targetBranch: str = Field(
        ...,
        description='TargetBranch is the branch to which hydrated manifests should be committed',
    )


class SyncSource(BaseModel):
    path: constr(pattern=r'^.{2,}|[^./]$', min_length=1) = Field(
        ...,
        description='Path is a directory path within the git repository where hydrated manifests should be committed to and synced\nfrom. The Path should never point to the root of the repo. If hydrateTo is set, this is just the path from which\nhydrated manifests will be synced.',
    )
    targetBranch: str = Field(
        ...,
        description='TargetBranch is the branch from which hydrated manifests will be synced.\nIf HydrateTo is not set, this is also the branch to which hydrated manifests are committed.',
    )


class SourceHydrator(BaseModel):
    drySource: DrySource = Field(
        ...,
        description='DrySource specifies where the dry "don\'t repeat yourself" manifest source lives.',
    )
    hydrateTo: HydrateTo | None = Field(
        default=None,
        description='HydrateTo specifies an optional "staging" location to push hydrated manifests to. An external system would then\nhave to move manifests to the SyncSource, e.g. by pull request.',
    )
    syncSource: SyncSource = Field(
        ..., description='SyncSource specifies where to sync hydrated manifests from.'
    )


class Jsonnet4(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory4(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet4 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter8(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm4(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter8] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch4(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize4(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch4] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter9(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin4(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter9] | None = None


class Source3(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory4 | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm4 | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize4 | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin4 | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class Automated(BaseModel):
    allowEmpty: bool | None = Field(
        default=None,
        description='AllowEmpty allows apps have zero live resources (default: false)',
    )
    enabled: bool | None = Field(
        default=None,
        description='Enable allows apps to explicitly control automated sync',
    )
    prune: bool | None = Field(
        default=None,
        description='Prune specifies whether to delete resources from the cluster that are not found in the sources anymore as part of automated sync (default: false)',
    )
    selfHeal: bool | None = Field(
        default=None,
        description='SelfHeal specifies whether to revert resources back to their desired state upon modification in the cluster (default: false)',
    )


class ManagedNamespaceMetadata(BaseModel):
    annotations: dict[str, str] | None = None
    labels: dict[str, str] | None = None


class Retry1(BaseModel):
    backoff: Backoff | None = Field(
        default=None,
        description='Backoff controls how to backoff on subsequent retries of failed syncs',
    )
    limit: int | None = Field(
        default=None,
        description='Limit is the maximum number of attempts for retrying a failed sync. If set to 0, no retries will be performed.',
    )
    refresh: bool | None = Field(
        default=None,
        description='Refresh indicates if the latest revision should be used on retry instead of the initial one (default: false)',
    )


class SyncPolicy(BaseModel):
    automated: Automated | None = Field(
        default=None,
        description='Automated will keep an application synced to the target revision',
    )
    managedNamespaceMetadata: ManagedNamespaceMetadata | None = Field(
        default=None,
        description='ManagedNamespaceMetadata controls metadata in the given namespace (if CreateNamespace=true)',
    )
    retry: Retry1 | None = Field(
        default=None, description='Retry controls failed sync retry behavior'
    )
    syncOptions: list[str] | None = Field(
        default=None, description='Options allow you to specify whole app sync-options'
    )


class Spec(BaseModel):
    destination: Destination = Field(
        ...,
        description='Destination is a reference to the target Kubernetes server and namespace',
    )
    ignoreDifferences: list[IgnoreDifference] | None = Field(
        default=None,
        description='IgnoreDifferences is a list of resources and their fields which should be ignored during comparison',
    )
    info: list[InfoItem] | None = Field(
        default=None,
        description='Info contains a list of information (URLs, email addresses, and plain text) that relates to the application',
    )
    project: str = Field(
        ...,
        description="Project is a reference to the project this application belongs to.\nThe empty string means that application belongs to the 'default' project.",
    )
    revisionHistoryLimit: int | None = Field(
        default=None,
        description="RevisionHistoryLimit limits the number of items kept in the application's revision history, which is used for informational purposes as well as for rollbacks to previous versions.\nThis should only be changed in exceptional circumstances.\nSetting to zero will store no history. This will reduce storage used.\nIncreasing will increase the space used to store the history, so we do not recommend increasing it.\nDefault is 10.",
    )
    source: Source2 | None = Field(
        default=None,
        description="Source is a reference to the location of the application's manifests or chart",
    )
    sourceHydrator: SourceHydrator | None = Field(
        default=None,
        description='SourceHydrator provides a way to push hydrated manifests back to git before syncing them to the cluster.',
    )
    sources: list[Source3] | None = Field(
        default=None,
        description="Sources is a reference to the location of the application's manifests or chart",
    )
    syncPolicy: SyncPolicy | None = Field(
        default=None,
        description='SyncPolicy controls when and how a sync will be performed',
    )


class Condition(BaseModel):
    lastTransitionTime: AwareDatetime | None = Field(
        default=None,
        description='LastTransitionTime is the time the condition was last observed',
    )
    message: str = Field(
        ...,
        description='Message contains human-readable message indicating details about condition',
    )
    type: str = Field(..., description='Type is an application condition type')


class Health(BaseModel):
    lastTransitionTime: AwareDatetime | None = Field(
        default=None,
        description='LastTransitionTime is the time the HealthStatus was set or updated',
    )
    message: str | None = Field(
        default=None,
        description='Message is a human-readable informational message describing the health status\n\nDeprecated: this field is not used and will be removed in a future release.',
    )
    status: str | None = Field(
        default=None, description='Status holds the status code of the application'
    )


class Jsonnet5(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory5(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet5 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter10(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm5(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter10] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch5(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize5(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch5] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter11(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin5(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter11] | None = None


class Source4(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory5 | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm5 | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize5 | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin5 | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class Jsonnet6(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory6(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet6 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter12(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm6(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter12] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch6(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize6(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch6] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter13(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin6(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter13] | None = None


class Source5(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory6 | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm6 | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize6 | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin6 | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class HistoryItem(BaseModel):
    deployStartedAt: AwareDatetime | None = Field(
        default=None,
        description='DeployStartedAt holds the time the sync operation started',
    )
    deployedAt: AwareDatetime = Field(
        ..., description='DeployedAt holds the time the sync operation completed'
    )
    id: int = Field(
        ..., description='ID is an auto incrementing identifier of the RevisionHistory'
    )
    initiatedBy: InitiatedBy | None = Field(
        default=None,
        description='InitiatedBy contains information about who initiated the operations',
    )
    revision: str | None = Field(
        default=None,
        description='Revision holds the revision the sync was performed against',
    )
    revisions: list[str] | None = Field(
        default=None,
        description='Revisions holds the revision of each source in sources field the sync was performed against',
    )
    source: Source4 | None = Field(
        default=None,
        description='Source is a reference to the application source used for the sync operation',
    )
    sources: list[Source5] | None = Field(
        default=None,
        description='Sources is a reference to the application sources used for the sync operation',
    )


class Retry2(BaseModel):
    backoff: Backoff | None = Field(
        default=None,
        description='Backoff controls how to backoff on subsequent retries of failed syncs',
    )
    limit: int | None = Field(
        default=None,
        description='Limit is the maximum number of attempts for retrying a failed sync. If set to 0, no retries will be performed.',
    )
    refresh: bool | None = Field(
        default=None,
        description='Refresh indicates if the latest revision should be used on retry instead of the initial one (default: false)',
    )


class Jsonnet7(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory7(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet7 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter14(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm7(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter14] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch7(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize7(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch7] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter15(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin7(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter15] | None = None


class Source6(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory7 | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm7 | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize7 | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin7 | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class Jsonnet8(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory8(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet8 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter16(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm8(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter16] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch8(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize8(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch8] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter17(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin8(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter17] | None = None


class Source7(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory8 | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm8 | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize8 | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin8 | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class SyncStrategy1(BaseModel):
    apply: Apply | None = Field(
        default=None,
        description='Apply will perform a `kubectl apply` to perform the sync.',
    )
    hook: Hook | None = Field(
        default=None,
        description='Hook will submit any referenced resources to perform the sync. This is the default strategy',
    )


class Sync1(BaseModel):
    autoHealAttemptsCount: int | None = Field(
        default=None,
        description='SelfHealAttemptsCount contains the number of auto-heal attempts',
    )
    dryRun: bool | None = Field(
        default=None,
        description='DryRun specifies to perform a `kubectl apply --dry-run` without actually performing the sync',
    )
    manifests: list[str] | None = Field(
        default=None,
        description='Manifests is an optional field that overrides sync source with a local directory for development',
    )
    prune: bool | None = Field(
        default=None,
        description='Prune specifies to delete resources from the cluster that are no longer tracked in git',
    )
    resources: list[Resource] | None = Field(
        default=None,
        description='Resources describes which resources shall be part of the sync',
    )
    revision: str | None = Field(
        default=None,
        description='Revision is the revision (Git) or chart version (Helm) which to sync the application to\nIf omitted, will use the revision specified in app spec.',
    )
    revisions: list[str] | None = Field(
        default=None,
        description='Revisions is the list of revision (Git) or chart version (Helm) which to sync each source in sources field for the application to\nIf omitted, will use the revision specified in app spec.',
    )
    source: Source6 | None = Field(
        default=None,
        description='Source overrides the source definition set in the application.\nThis is typically set in a Rollback operation and is nil during a Sync operation',
    )
    sources: list[Source7] | None = Field(
        default=None,
        description='Sources overrides the source definition set in the application.\nThis is typically set in a Rollback operation and is nil during a Sync operation',
    )
    syncOptions: list[str] | None = Field(
        default=None,
        description='SyncOptions provide per-sync sync-options, e.g. Validate=false',
    )
    syncStrategy: SyncStrategy1 | None = Field(
        default=None, description='SyncStrategy describes how to perform the sync'
    )


class Operation1(BaseModel):
    info: list[InfoItem] | None = Field(
        default=None,
        description='Info is a list of informational items for this operation',
    )
    initiatedBy: InitiatedBy | None = Field(
        default=None,
        description='InitiatedBy contains information about who initiated the operations',
    )
    retry: Retry2 | None = Field(
        default=None, description='Retry controls the strategy to apply if a sync fails'
    )
    sync: Sync1 | None = Field(
        default=None, description='Sync contains parameters for the operation'
    )


class Resource2(BaseModel):
    group: str = Field(..., description='Group specifies the API group of the resource')
    hookPhase: str | None = Field(
        default=None,
        description='HookPhase contains the state of any operation associated with this resource OR hook\nThis can also contain values for non-hook resources.',
    )
    hookType: str | None = Field(
        default=None,
        description='HookType specifies the type of the hook. Empty for non-hook resources',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images contains the images related to the ResourceResult',
    )
    kind: str = Field(..., description='Kind specifies the API kind of the resource')
    message: str | None = Field(
        default=None,
        description='Message contains an informational or error message for the last sync OR operation',
    )
    name: str = Field(..., description='Name specifies the name of the resource')
    namespace: str = Field(
        ..., description='Namespace specifies the target namespace of the resource'
    )
    status: str | None = Field(
        default=None,
        description='Status holds the final result of the sync. Will be empty if the resources is yet to be applied/pruned and is always zero-value for hooks',
    )
    syncPhase: str | None = Field(
        default=None,
        description='SyncPhase indicates the particular phase of the sync that this result was acquired in',
    )
    version: str = Field(
        ..., description='Version specifies the API version of the resource'
    )


class Jsonnet9(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory9(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet9 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter18(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm9(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter18] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch9(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize9(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch9] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter19(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin9(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter19] | None = None


class Source8(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory9 | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm9 | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize9 | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin9 | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class Jsonnet10(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory10(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet10 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter20(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm10(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter20] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch10(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize10(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch10] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter21(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin10(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter21] | None = None


class Source9(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory10 | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm10 | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize10 | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin10 | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class SyncResult(BaseModel):
    managedNamespaceMetadata: ManagedNamespaceMetadata | None = Field(
        default=None,
        description='ManagedNamespaceMetadata contains the current sync state of managed namespace metadata',
    )
    resources: list[Resource2] | None = Field(
        default=None,
        description='Resources contains a list of sync result items for each individual resource in a sync operation',
    )
    revision: str = Field(
        ...,
        description='Revision holds the revision this sync operation was performed to',
    )
    revisions: list[str] | None = Field(
        default=None,
        description='Revisions holds the revision this sync operation was performed for respective indexed source in sources field',
    )
    source: Source8 | None = Field(
        default=None,
        description='Source records the application source information of the sync, used for comparing auto-sync',
    )
    sources: list[Source9] | None = Field(
        default=None,
        description='Source records the application source information of the sync, used for comparing auto-sync',
    )


class OperationState(BaseModel):
    finishedAt: AwareDatetime | None = Field(
        default=None, description='FinishedAt contains time of operation completion'
    )
    message: str | None = Field(
        default=None,
        description='Message holds any pertinent messages when attempting to perform operation (typically errors).',
    )
    operation: Operation1 = Field(
        ..., description='Operation is the original requested operation'
    )
    phase: str = Field(..., description='Phase is the current phase of the operation')
    retryCount: int | None = Field(
        default=None, description='RetryCount contains time of operation retries'
    )
    startedAt: AwareDatetime = Field(
        ..., description='StartedAt contains time of operation start'
    )
    syncResult: SyncResult | None = Field(
        default=None, description='SyncResult is the result of a Sync operation'
    )


class Health1(BaseModel):
    lastTransitionTime: AwareDatetime | None = Field(
        default=None,
        description='LastTransitionTime is the time the HealthStatus was set or updated\n\nDeprecated: this field is not used and will be removed in a future release.',
    )
    message: str | None = Field(
        default=None,
        description='Message is a human-readable informational message describing the health status',
    )
    status: str | None = Field(
        default=None, description='Status holds the status code of the resource'
    )


class Resource3(BaseModel):
    group: str | None = Field(
        default=None,
        description='Group represents the API group of the resource (e.g., "apps" for Deployments).',
    )
    health: Health1 | None = Field(
        default=None,
        description='Health indicates the health status of the resource (e.g., Healthy, Degraded, Progressing).',
    )
    hook: bool | None = Field(
        default=None,
        description='Hook is true if the resource is used as a lifecycle hook in an Argo CD application.',
    )
    kind: str | None = Field(
        default=None,
        description='Kind specifies the type of the resource (e.g., "Deployment", "Service").',
    )
    name: str | None = Field(
        default=None,
        description='Name is the unique name of the resource within the namespace.',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace defines the Kubernetes namespace where the resource is located.',
    )
    requiresDeletionConfirmation: bool | None = Field(
        default=None,
        description='RequiresDeletionConfirmation is true if the resource requires explicit user confirmation before deletion.',
    )
    requiresPruning: bool | None = Field(
        default=None,
        description='RequiresPruning is true if the resource needs to be pruned (deleted) as part of synchronization.',
    )
    status: str | None = Field(
        default=None,
        description='Status represents the synchronization state of the resource (e.g., Synced, OutOfSync).',
    )
    syncWave: int | None = Field(
        default=None,
        description='SyncWave determines the order in which resources are applied during a sync operation.\nLower values are applied first.',
    )
    version: str | None = Field(
        default=None,
        description='Version indicates the API version of the resource (e.g., "v1", "v1beta1").',
    )


class Phase(StrEnum):
    Hydrating = 'Hydrating'
    Failed = 'Failed'
    Hydrated = 'Hydrated'


class Jsonnet11(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory11(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet11 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter22(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm11(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter22] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch11(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize11(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch11] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter23(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin11(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter23] | None = None


class DrySource1(BaseModel):
    directory: Directory11 | None = Field(
        default=None, description='Directory specifies path/directory specific options'
    )
    helm: Helm11 | None = Field(
        default=None, description='Helm specifies helm specific options'
    )
    kustomize: Kustomize11 | None = Field(
        default=None, description='Kustomize specifies kustomize specific options'
    )
    path: str = Field(
        ...,
        description='Path is a directory path within the Git repository where the manifests are located',
    )
    plugin: Plugin11 | None = Field(
        default=None,
        description='Plugin specifies config management plugin specific options',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the git repository that contains the application manifests',
    )
    targetRevision: str = Field(
        ..., description='TargetRevision defines the revision of the source to hydrate'
    )


class SourceHydrator2(BaseModel):
    drySource: DrySource1 = Field(
        ...,
        description='DrySource specifies where the dry "don\'t repeat yourself" manifest source lives.',
    )
    hydrateTo: HydrateTo | None = Field(
        default=None,
        description='HydrateTo specifies an optional "staging" location to push hydrated manifests to. An external system would then\nhave to move manifests to the SyncSource, e.g. by pull request.',
    )
    syncSource: SyncSource = Field(
        ..., description='SyncSource specifies where to sync hydrated manifests from.'
    )


class CurrentOperation(BaseModel):
    drySHA: str | None = Field(
        default=None,
        description='DrySHA holds the resolved revision (sha) of the dry source as of the most recent reconciliation',
    )
    finishedAt: AwareDatetime | None = Field(
        default=None,
        description='FinishedAt indicates when the hydrate operation finished',
    )
    hydratedSHA: str | None = Field(
        default=None,
        description='HydratedSHA holds the resolved revision (sha) of the hydrated source as of the most recent reconciliation',
    )
    message: str = Field(
        ...,
        description='Message contains a message describing the current status of the hydrate operation',
    )
    phase: Phase = Field(
        ..., description='Phase indicates the status of the hydrate operation'
    )
    sourceHydrator: SourceHydrator2 | None = Field(
        default=None,
        description='SourceHydrator holds the hydrator config used for the hydrate operation',
    )
    startedAt: AwareDatetime | None = Field(
        default=None,
        description='StartedAt indicates when the hydrate operation started',
    )


class Jsonnet12(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory12(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet12 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter24(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm12(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter24] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch12(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize12(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch12] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter25(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin12(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter25] | None = None


class DrySource2(BaseModel):
    directory: Directory12 | None = Field(
        default=None, description='Directory specifies path/directory specific options'
    )
    helm: Helm12 | None = Field(
        default=None, description='Helm specifies helm specific options'
    )
    kustomize: Kustomize12 | None = Field(
        default=None, description='Kustomize specifies kustomize specific options'
    )
    path: str = Field(
        ...,
        description='Path is a directory path within the Git repository where the manifests are located',
    )
    plugin: Plugin12 | None = Field(
        default=None,
        description='Plugin specifies config management plugin specific options',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the git repository that contains the application manifests',
    )
    targetRevision: str = Field(
        ..., description='TargetRevision defines the revision of the source to hydrate'
    )


class SourceHydrator3(BaseModel):
    drySource: DrySource2 = Field(
        ...,
        description='DrySource specifies where the dry "don\'t repeat yourself" manifest source lives.',
    )
    hydrateTo: HydrateTo | None = Field(
        default=None,
        description='HydrateTo specifies an optional "staging" location to push hydrated manifests to. An external system would then\nhave to move manifests to the SyncSource, e.g. by pull request.',
    )
    syncSource: SyncSource = Field(
        ..., description='SyncSource specifies where to sync hydrated manifests from.'
    )


class LastSuccessfulOperation(BaseModel):
    drySHA: str | None = Field(
        default=None,
        description='DrySHA holds the resolved revision (sha) of the dry source as of the most recent reconciliation',
    )
    hydratedSHA: str | None = Field(
        default=None,
        description='HydratedSHA holds the resolved revision (sha) of the hydrated source as of the most recent reconciliation',
    )
    sourceHydrator: SourceHydrator3 | None = Field(
        default=None,
        description='SourceHydrator holds the hydrator config used for the hydrate operation',
    )


class SourceHydrator1(BaseModel):
    currentOperation: CurrentOperation | None = Field(
        default=None,
        description='CurrentOperation holds the status of the hydrate operation',
    )
    lastSuccessfulOperation: LastSuccessfulOperation | None = Field(
        default=None,
        description='LastSuccessfulOperation holds info about the most recent successful hydration',
    )


class Summary(BaseModel):
    externalURLs: list[str] | None = Field(
        default=None,
        description='ExternalURLs holds all external URLs of application child resources.',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images holds all images of application child resources.',
    )


class Jsonnet13(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory13(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet13 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter26(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm13(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter26] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch13(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize13(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch13] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter27(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin13(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter27] | None = None


class Source10(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory13 | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm13 | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize13 | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin13 | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class Jsonnet14(BaseModel):
    extVars: list[ExtVar] | None = Field(
        default=None, description='ExtVars is a list of Jsonnet External Variables'
    )
    libs: list[str] | None = Field(
        default=None, description='Additional library search dirs'
    )
    tlas: list[Tla] | None = Field(
        default=None, description='TLAS is a list of Jsonnet Top-level Arguments'
    )


class Directory14(BaseModel):
    exclude: str | None = Field(
        default=None,
        description='Exclude contains a glob pattern to match paths against that should be explicitly excluded from being used during manifest generation',
    )
    include: str | None = Field(
        default=None,
        description='Include contains a glob pattern to match paths against that should be explicitly included during manifest generation',
    )
    jsonnet: Jsonnet14 | None = Field(
        default=None, description='Jsonnet holds options specific to Jsonnet'
    )
    recurse: bool | None = Field(
        default=None,
        description='Recurse specifies whether to scan a directory recursively for manifests',
    )


class Parameter28(BaseModel):
    forceString: bool | None = Field(
        default=None,
        description='ForceString determines whether to tell Helm to interpret booleans and numbers as strings',
    )
    name: str | None = Field(
        default=None, description='Name is the name of the Helm parameter'
    )
    value: str | None = Field(
        default=None, description='Value is the value for the Helm parameter'
    )


class Helm14(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    fileParameters: list[FileParameter] | None = Field(
        default=None,
        description='FileParameters are file parameters to the helm template',
    )
    ignoreMissingValueFiles: bool | None = Field(
        default=None,
        description='IgnoreMissingValueFiles prevents helm template from failing when valueFiles do not exist locally by not appending them to helm template --values',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    namespace: str | None = Field(
        default=None,
        description="Namespace is an optional namespace to template with. If left empty, defaults to the app's destination namespace.",
    )
    parameters: list[Parameter28] | None = Field(
        default=None,
        description='Parameters is a list of Helm parameters which are passed to the helm template command upon manifest generation',
    )
    passCredentials: bool | None = Field(
        default=None,
        description="PassCredentials pass credentials to all domains (Helm's --pass-credentials)",
    )
    releaseName: str | None = Field(
        default=None,
        description='ReleaseName is the Helm release name to use. If omitted it will use the application name',
    )
    skipCrds: bool | None = Field(
        default=None,
        description="SkipCrds skips custom resource definition installation step (Helm's --skip-crds)",
    )
    skipSchemaValidation: bool | None = Field(
        default=None,
        description="SkipSchemaValidation skips JSON schema validation (Helm's --skip-schema-validation)",
    )
    skipTests: bool | None = Field(
        default=None,
        description="SkipTests skips test manifest installation step (Helm's --skip-tests).",
    )
    valueFiles: list[str] | None = Field(
        default=None,
        description='ValuesFiles is a list of Helm value files to use when generating a template',
    )
    values: str | None = Field(
        default=None,
        description='Values specifies Helm values to be passed to helm template, typically defined as a block. ValuesObject takes precedence over Values, so use one or the other.',
    )
    valuesObject: dict[str, Any] | None = Field(
        default=None,
        description='ValuesObject specifies Helm values to be passed to helm template, defined as a map. This takes precedence over Values.',
    )
    version: str | None = Field(
        default=None,
        description='Version is the Helm version to use for templating ("3")',
    )


class Patch14(BaseModel):
    options: dict[str, bool] | None = None
    patch: str | None = None
    path: str | None = None
    target: Target | None = None


class Kustomize14(BaseModel):
    apiVersions: list[str] | None = Field(
        default=None,
        description='APIVersions specifies the Kubernetes resource API versions to pass to Helm when templating manifests. By default,\nArgo CD uses the API versions of the target cluster. The format is [group/]version/kind.',
    )
    commonAnnotations: dict[str, str] | None = Field(
        default=None,
        description='CommonAnnotations is a list of additional annotations to add to rendered manifests',
    )
    commonAnnotationsEnvsubst: bool | None = Field(
        default=None,
        description='CommonAnnotationsEnvsubst specifies whether to apply env variables substitution for annotation values',
    )
    commonLabels: dict[str, str] | None = Field(
        default=None,
        description='CommonLabels is a list of additional labels to add to rendered manifests',
    )
    components: list[str] | None = Field(
        default=None,
        description='Components specifies a list of kustomize components to add to the kustomization before building',
    )
    forceCommonAnnotations: bool | None = Field(
        default=None,
        description='ForceCommonAnnotations specifies whether to force applying common annotations to resources for Kustomize apps',
    )
    forceCommonLabels: bool | None = Field(
        default=None,
        description='ForceCommonLabels specifies whether to force applying common labels to resources for Kustomize apps',
    )
    ignoreMissingComponents: bool | None = Field(
        default=None,
        description='IgnoreMissingComponents prevents kustomize from failing when components do not exist locally by not appending them to kustomization file',
    )
    images: list[str] | None = Field(
        default=None,
        description='Images is a list of Kustomize image override specifications',
    )
    kubeVersion: str | None = Field(
        default=None,
        description='KubeVersion specifies the Kubernetes API version to pass to Helm when templating manifests. By default, Argo CD\nuses the Kubernetes version of the target cluster.',
    )
    labelIncludeTemplates: bool | None = Field(
        default=None,
        description='LabelIncludeTemplates specifies whether to apply common labels to resource templates or not',
    )
    labelWithoutSelector: bool | None = Field(
        default=None,
        description='LabelWithoutSelector specifies whether to apply common labels to resource selectors or not',
    )
    namePrefix: str | None = Field(
        default=None,
        description='NamePrefix overrides the namePrefix in the kustomization.yaml for Kustomize apps',
    )
    nameSuffix: str | None = Field(
        default=None,
        description='NameSuffix overrides the nameSuffix in the kustomization.yaml for Kustomize apps',
    )
    namespace: str | None = Field(
        default=None,
        description='Namespace sets the namespace that Kustomize adds to all resources',
    )
    patches: list[Patch14] | None = Field(
        default=None, description='Patches is a list of Kustomize patches'
    )
    replicas: list[Replica] | None = Field(
        default=None,
        description='Replicas is a list of Kustomize Replicas override specifications',
    )
    version: str | None = Field(
        default=None,
        description='Version controls which version of Kustomize to use for rendering manifests',
    )


class Parameter29(BaseModel):
    array: list[str] | None = Field(
        default=None, description='Array is the value of an array type parameter.'
    )
    map: dict[str, str] | None = Field(
        default=None, description='Map is the value of a map type parameter.'
    )
    name: str | None = Field(
        default=None, description='Name is the name identifying a parameter.'
    )
    string: str | None = Field(
        default=None, description='String_ is the value of a string type parameter.'
    )


class Plugin14(BaseModel):
    env: list[EnvItem] | None = Field(
        default=None, description='Env is a list of environment variable entries'
    )
    name: str | None = None
    parameters: list[Parameter29] | None = None


class Source11(BaseModel):
    chart: str | None = Field(
        default=None,
        description='Chart is a Helm chart name, and must be specified for applications sourced from a Helm repo.',
    )
    directory: Directory14 | None = Field(
        default=None, description='Directory holds path/directory specific options'
    )
    helm: Helm14 | None = Field(
        default=None, description='Helm holds helm specific options'
    )
    kustomize: Kustomize14 | None = Field(
        default=None, description='Kustomize holds kustomize specific options'
    )
    name: str | None = Field(
        default=None,
        description='Name is used to refer to a source and is displayed in the UI. It is used in multi-source Applications.',
    )
    path: str | None = Field(
        default=None,
        description='Path is a directory path within the Git repository, and is only valid for applications sourced from Git.',
    )
    plugin: Plugin14 | None = Field(
        default=None,
        description='Plugin holds config management plugin specific options',
    )
    ref: str | None = Field(
        default=None,
        description='Ref is reference to another source within sources field. This field will not be used if used with a `source` tag.',
    )
    repoURL: str = Field(
        ...,
        description='RepoURL is the URL to the repository (Git or Helm) that contains the application manifests',
    )
    targetRevision: str | None = Field(
        default=None,
        description="TargetRevision defines the revision of the source to sync the application to.\nIn case of Git, this can be commit, tag, or branch. If omitted, will equal to HEAD.\nIn case of Helm, this is a semver tag for the Chart's version.",
    )


class ComparedTo(BaseModel):
    destination: Destination = Field(
        ...,
        description="Destination is a reference to the application's destination used for comparison",
    )
    ignoreDifferences: list[IgnoreDifference] | None = Field(
        default=None,
        description="IgnoreDifferences is a reference to the application's ignored differences used for comparison",
    )
    source: Source10 | None = Field(
        default=None,
        description="Source is a reference to the application's source used for comparison",
    )
    sources: list[Source11] | None = Field(
        default=None,
        description="Sources is a reference to the application's multiple sources used for comparison",
    )


class Sync2(BaseModel):
    comparedTo: ComparedTo | None = Field(
        default=None,
        description='ComparedTo contains information about what has been compared',
    )
    revision: str | None = Field(
        default=None,
        description='Revision contains information about the revision the comparison has been performed to',
    )
    revisions: list[str] | None = Field(
        default=None,
        description='Revisions contains information about the revisions of multiple sources the comparison has been performed to',
    )
    status: str = Field(..., description='Status is the sync state of the comparison')


class Status(BaseModel):
    conditions: list[Condition] | None = Field(
        default=None,
        description='Conditions is a list of currently observed application conditions',
    )
    controllerNamespace: str | None = Field(
        default=None,
        description='ControllerNamespace indicates the namespace in which the application controller is located',
    )
    health: Health | None = Field(
        default=None,
        description="Health contains information about the application's current health status",
    )
    history: list[HistoryItem] | None = Field(
        default=None,
        description="History contains information about the application's sync history",
    )
    observedAt: AwareDatetime | None = Field(
        default=None,
        description='ObservedAt indicates when the application state was updated without querying latest git state\n\nDeprecated: controller no longer updates ObservedAt field',
    )
    operationState: OperationState | None = Field(
        default=None,
        description='OperationState contains information about any ongoing operations, such as a sync',
    )
    reconciledAt: AwareDatetime | None = Field(
        default=None,
        description='ReconciledAt indicates when the application state was reconciled using the latest git version',
    )
    resourceHealthSource: str | None = Field(
        default=None,
        description='ResourceHealthSource indicates where the resource health status is stored: inline if not set or appTree',
    )
    resources: list[Resource3] | None = Field(
        default=None,
        description='Resources is a list of Kubernetes resources managed by this application',
    )
    sourceHydrator: SourceHydrator1 | None = Field(
        default=None,
        description='SourceHydrator stores information about the current state of source hydration',
    )
    sourceType: str | None = Field(
        default=None, description='SourceType specifies the type of this application'
    )
    sourceTypes: list[str] | None = Field(
        default=None,
        description='SourceTypes specifies the type of the sources included in the application',
    )
    summary: Summary | None = Field(
        default=None,
        description='Summary contains a list of URLs and container images used by this application',
    )
    sync: Sync2 | None = Field(
        default=None,
        description="Sync contains information about the application's current sync status",
    )


class Application(BaseModel):
    apiVersion: str | None = Field(
        default=None,
        description='APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources',
    )
    kind: str | None = Field(
        default=None,
        description='Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds',
    )
    metadata: dict[str, Any]
    operation: Operation | None = Field(
        default=None,
        description='Operation contains information about a requested or running operation',
    )
    spec: Spec = Field(
        ...,
        description='ApplicationSpec represents desired application state. Contains link to repository with application definition and additional parameters link definition revision.',
    )
    status: Status | None = Field(
        default=None,
        description='ApplicationStatus contains status information for the application',
    )
